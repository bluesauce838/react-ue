import React, { useState } from 'react';

export default function App() {
  const [state, setState] = useState(0);
  function handleClick() {
    setState: (name, age) => {
      console.log(rahul, 10);
      (rahul, 10)
    };
  
  }


  function mathd (){
    math.random()
  }

  return (
    <div>
      <p>click button {state}</p>
      <button onClick={handleClick}>hjk</button>
    </div>
  );
}
